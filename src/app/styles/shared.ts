import styled from 'styled-components';

export const Overlay = styled.div`
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  position: fixed;
  z-index: 101;
  background: rgba(0, 0, 0, 0.78);
  display: none;
`;
